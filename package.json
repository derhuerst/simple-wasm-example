{
	"private": true,
	"name": "simple-wasm-example",
	"description": "A Rust lib, compiled to WASM, with fallback to asm.js.",
	"version": "0.1.0",
	"main": "index.js",
	"files": [
		"index.js",
		"src",
		"target/asmjs-unknown-emscripten/hello_world.js",
		"target/wasm32-unknown-emscripten/hello_world.js",
		"target/wasm32-unknown-emscripten/hello_world.wasm"
	],
	"keywords": [
		"wasm",
		"rust",
		"asm.js",
		"webassembly"
	],
	"author": "Jannis R <mail@jannisr.de>",
	"homepage": "https://github.com/derhuerst/simple-wasm-example",
	"repository": "derhuerst/simple-wasm-example",
	"bugs": "https://github.com/derhuerst/simple-wasm-example/issues",
	"license": "ISC",
	"engines": {
		"node": ">=6"
	},
	"dependencies": {
	},
	"devDependencies": {
	},
	"scripts": {
		"build:dev": "cargo build --target asmjs-unknown-emscripten && cargo build --target wasm32-unknown-emscripten",
		"build": "cargo build --target asmjs-unknown-emscripten --release && cargo build --target wasm32-unknown-emscripten --release",
		"prepublishOnly": "npm test"
	}
}
